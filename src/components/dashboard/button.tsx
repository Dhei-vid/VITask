const Button = () => {
  return (
    <div
      className={
        "cursor-pointer flex items-center justify-center rounded-full bg-color py-1 px-6"
      }
    >
      <p className={"text-sm text-white"}>View</p>
    </div>
  );
};

export default Button;
